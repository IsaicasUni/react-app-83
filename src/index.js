import React from 'react';
import ReactDOM from 'react-dom';
// Llama el contenido de la constante
import Datos from './Componente';

// Imprime los valores de la tabla
ReactDOM.render(
  <Datos />,
  document.getElementById('root')
);

